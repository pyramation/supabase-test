# RLS Testing

```
npx supabase init
npx supabase start
```

```
pnpm install
cd packages/base32
export PGPORT=54322
export PGHOST=localhost
export PGUSER=postgres
export PGPASSWORD=postgres

pnpm test
```

